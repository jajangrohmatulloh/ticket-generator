import React from 'react';
import { Button, ButtonGroup } from '@chakra-ui/react';

const ButtonComp = () => {
  return <Button colorScheme="blue">Create a new ticket + </Button>;
};

export default ButtonComp;
